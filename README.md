# BirdTok app
## pre-commit hook installation
* In order to install pre-commit hooks in bash execute following commands:
1. `sudo apt install pre-commit`
2. `pre-commit install` inside of the folder with .pre-commit-config.yaml
3. Done
